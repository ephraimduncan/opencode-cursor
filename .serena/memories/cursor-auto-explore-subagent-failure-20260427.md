# Cursor auto explore subagent failure follow-up (2026-04-27)

Context: Repo `C:\NEW PRG\opencode-cursor`. User asked to diagnose and fix `explore` subagent failures: `[Error: Connect error invalid_argument: Model details are required]`. User explicitly rejected workaround of changing `explore.model` from `cursor/auto` to explicit model.

Findings:
- Initial explore tasks failed immediately before search.
- Active OMO config has `explore.model = "cursor/auto"`.
- Prior code omitted `AgentRunRequest.modelDetails` when modelId was `auto`.
- Cursor private `agent.v1.AgentService/Run` requires `modelDetails`; public Cloud Agents REST `model: "default"`/omitted semantics do not apply.
- External OSS examples consistently send `modelDetails` with explicit model IDs; `requestedModel` examples also include `modelDetails`.

Implemented:
- `src/proxy.ts`: added `resolveCursorRunModelId(modelId)`. Keeps `auto` exposed in `/v1/models`, but maps `model: "auto"` to the first discovered non-auto Cursor model before building `ModelDetails`. Explicit models unchanged.
- `test/smoke.ts`: fake backend now observes `AgentService/Run`; added smoke test that auto includes `modelDetails` and resolves to `composer-2` fixture; explicit `composer-2` still forwards unchanged.
- `dist/proxy.js` regenerated by build.

Verified:
- LSP diagnostics on `src/proxy.ts` and `test/smoke.ts`: no diagnostics.
- `bun test/smoke.ts`: passed.
- `bun run build`: passed.

Important: A real explore task launched in the same OpenCode session after the fix (`bg_a60161ad`) still failed with `Model details are required`. Follow-up inspection showed `src/index.ts` already calls `startProxy(..., models)`, so likely current OpenCode process has old plugin/proxy code loaded in memory. Need restart/reload OpenCode, then re-test explore. Do not change OMO config to explicit model unless user reverses instruction.

Session docs:
- `docs/sessions/20260427_193435_cursor_auto_discovered_model.md`
- `docs/sessions/20260427_193900_explore_subagent_auto_followup.md`
